# AGENT.md — RSS × AI 微信小程序（可迁移到 RN App）开发指南

> 目标：先把微信小程序 V1.0 MVP 跑通并验证需求，同时在实现上为后续上线 Android / iOS（React Native）降低迁移成本。

---

## 0. 核心原则（迁移成本最低的做法）

1. **业务能力全部放后端**：RSS 抓取、AI 摘要/标签、去重、低质过滤、源校验、热度计算都在服务端。
2. **前端只是壳**：小程序只负责 UI、交互、渲染、轻量本地缓存。
3. **接口协议冻结**：从 V1 开始就做 `API /api/v1` 版本化、分页 cursor 化、错误码统一。
4. **身份模型抽象**：不把“微信登录”写死为唯一身份，统一为 `user_id + identities`。
5. **埋点跨端一致**：事件名与字段设计一次定好，后续 RN 直接复用口径。
6. **外链统一中转策略**：小程序与 App 同一套“中转页”语义（显示来源 + 风险提示 + 再跳转）。
7. **API和数据库设计**： 请尽量遵循docs/API和DB设计.md文档，如果有问题请找我确认。

---

## 1. 产品范围（V1.0 必做 / 不做）

### 1.1 V1.0 必做（MVP 闭环）

* 源库：精选源、分类、排序、有效性校验（后台每日）
* 找源与订阅：搜索、订阅/取消、订阅列表排序、置顶（≤3）
* 阅读体验：聚合阅读流、筛选（源/标签/时间）、下拉刷新
* AI：摘要（50–80 字默认）、标签（2–3）、去重、低质过滤
* 收藏与分享：收藏列表、微信分享
* 合规：协议弹窗、举报（内容/源）
* 登录：默认游客，本地保存；可选微信登录做云端同步（可后置）
* 埋点：核心漏斗事件 + 性能字段

### 1.2 V1.0 不做（明确 Out of Scope）

* 用户自提交 RSS 源
* 深度推荐算法 / 画像
* 付费订阅
* 评论社交
* 全文离线/全文解析（先跳源站）

---

## 2. 技术架构（建议）

### 2.1 后端（长期核心）

* Python + FastAPI
* MySQL（业务数据）
* Redis（缓存 + 队列 broker）
* Celery（AI/去重/异步）+ Celery Beat（定时抓取）

> 前期 demo 阶段可用 SQLite 代替 MySQL，便于快速验证并减少一个服务器；功能跑通后再切回 MySQL。

> MVP 禁止一上来 Kafka / ES / 向量库。先用 MySQL + Redis + Celery 跑通。

### 2.2 小程序（短期壳）

* 页面：阅读｜找源｜订阅｜我的
* 本地缓存：订阅/收藏/设置（游客）
* API Client：统一封装（便于 RN 迁移）

### 2.3 后续 RN（迁移策略）

* React Native（优先 Expo，除非必须深度原生能力）
* 复用：API、数据结构、埋点事件模型

---

## 3. 关键数据与协议（从 V1 就要“对齐 App”）

### 3.1 API 规范（强制）

* Base：`/api/v1/*`
* 统一响应：`{ code, message, data, trace_id }`
* 统一分页：`{ items: [], next_cursor, has_more }`
* 统一时间：ISO 8601（UTC）
* 统一错误码：

  * 400 参数错误
  * 401 未授权
  * 429 限流
  * 5xx 服务端错误

### 3.2 身份模型（避免绑死微信）

**后端必须存在：**

* `user_id`：唯一用户主体
* `device_id`：匿名游客标识（小程序可用本地生成/持久化）
* `identity`：登录方式绑定（wx_openid / apple_sub / google_sub / email/phone）

V1 小程序实现建议：

* 默认游客：客户端生成 `device_id`，请求头携带
* 可选微信登录：后端将 `wx_openid` 绑定到同一个 `user_id`，并把游客数据迁移（订阅/收藏）

### 3.3 埋点事件（跨端复用）

**必埋事件（V1）**

* `app_open`
* `source_view`（源卡曝光/点击）
* `subscribe_add` / `subscribe_remove`
* `feed_view`
* `content_view`
* `click_fulltext`
* `favorite_add` / `favorite_remove`
* `share_click` / `share_success`

**公共字段（必须）**

* `platform`：wx / ios / android
* `app_version`
* `device_id` / `user_id` / `session_id`
* `latency_ms`
* `ai_ready`

---

## 4. 服务端任务与 AI 分级处理（降本核心）

### 4.1 RSS 抓取与入库

* 频率：按源更新频率分组调度（高/中/低）
* 幂等：`UNIQUE(source_id, link_url)`
* 失败计数：累计失败→状态变更

### 4.2 源有效性校验（每日）

* 48h 无更新 → `updating`
* 7 天抓取失败 → `invalid`（前端隐藏）

### 4.3 AI 分级队列（必须）

* P0：置顶源 + 全局热门源（Top N）→ 近实时摘要/标签/过滤
* P1：普通订阅源最新 24h → 延迟处理
* P2：非订阅或历史内容 → 不处理或只摘要

### 4.4 降级策略（必须）

* 限额触发：只做摘要，不做标签/过滤
* AI 失败：回退为标题 + RSS 原摘要

---

## 5. 去重策略（V1 工程可落地）

V1 先做“够用”的去重：

1. 链接规范化（去 utm 等）
2. 标题归一化（去站点后缀、空白、标点）
3. 指纹（SimHash/MinHash）快速判断重复
4. 命中重复：保留最新或来源权重更高的一条，其他记录 `duplicate_of_id`

---

## 6. 小程序实现要点（为 RN 迁移做准备）

### 6.1 统一 API Client

* 小程序内封装一个 `client`：

  * 自动加 headers（device_id、session_id、app_version、platform）
  * 统一错误处理与重试策略
  * 统一分页 cursor 处理

> 未来 RN 端只需要复制这套 client 逻辑。

### 6.2 页面与状态

* 列表必须支持：

  * cursor 分页
  * 下拉刷新
  * 本地缓存最近 N 条（可选）
* 不阻塞 AI：`ai_ready=false` 先展示“生成中”占位

### 6.3 中转页

* 详情页底部展示：来源、举报
* “查看全文”跳转到中转页（显示来源+风险提示）再跳源站

---

## 7. 安全、合规与审核注意事项

* 首次启动弹窗：隐私协议 / 用户协议（强制同意）
* 举报入口：内容与源都要有
* 记录来源：内容详情页固定展示 RSS 源名
* 外链：必须用户点击确认，不做自动跳转

---

## 8. 开发顺序（严格按闭环推进）

1. 基础数据与接口：源库/订阅/阅读流/详情
2. RSS 抓取 + 入库 + 校验（先让内容流动）
3. AI 队列：摘要→标签→过滤→去重（异步不阻塞）
4. 收藏/分享/举报/协议
5. 埋点打通 + 简单指标看板

> 任何时候如果闭环没跑通，不允许开始做 V2 功能。

---

## 9. 质量门槛（上线前检查清单）

* [ ] 新用户 30 秒内可完成首次订阅
* [ ] 阅读流首屏 < 1s（有缓存更佳）
* [ ] AI 失败可回退，不影响阅读
* [ ] RSS 抓取幂等，重复写入不会膨胀
* [ ] 失效源前端隐藏，状态可见
* [ ] 关键埋点可用（至少漏斗 8 个事件）

---

## 10. 未来迁移到 RN App 的最小新增项

* 登录扩展：Apple/Google（保持同一 `user_id`）
* 推送：`device_token` 表 + token 上报接口
* 本地缓存：SQLite/Realm（阅读流/收藏/设置）
* 分享：系统 Share Sheet（保持 share_link 追踪参数）

---

## 11. 决策记录（冻结项）

* 源库：官方维护（不开放用户提交）
* AI：分级处理（热门/置顶优先）
* 外链：安全中转页
* 数据：V1 即埋点
* 后端：Python + FastAPI
* 未来 App：React Native（倾向）
